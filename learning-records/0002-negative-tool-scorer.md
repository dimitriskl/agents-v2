# Negative Tool Scorer

The user implemented `toolsAvoided` correctly: a negative single-turn evaluator returns `0` when any selected tool is forbidden and `1` otherwise. Future eval lessons can assume the user understands strict negative scoring and move on to strict positive scoring and Laminar wiring.
