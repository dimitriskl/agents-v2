# Evals Prior Knowledge

The existing session resume shows the user understands that `data` is the exam question, `target` is the answer key, negative evals check that inappropriate tools are not selected, and mock tools isolate tool selection from tool execution. Future lessons can start at evaluator design instead of re-teaching why mocked single-turn evals exist.
