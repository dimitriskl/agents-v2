# Strict Positive Tool Scorer

The user corrected `toolsSelected` from an "any expected tool matched" rule to an "every expected tool matched" rule. Local hard-coded checks passed for exact selection, missing required tools, clean forbidden-tool avoidance, and forbidden-tool violation, so future lessons can treat strict single-turn scorer semantics as understood.
